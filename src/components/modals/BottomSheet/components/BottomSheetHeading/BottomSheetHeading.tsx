import React from 'react';
import { Heading } from './styled';

const BottomSheetHeading = ({ heading }: { heading: string }) => (
  <Heading>{heading}</Heading>
);

export default BottomSheetHeading;
