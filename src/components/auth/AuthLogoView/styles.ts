import { StyleSheet } from 'react-native';
import { hp } from '../../../styles/metrics';

export default StyleSheet.create({
  screen: {
    flex: 1,
  },
  logoContainer: {
    alignItems: 'center',
    paddingBottom: hp(5),
  },
});
